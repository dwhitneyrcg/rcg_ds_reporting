# This is a Databricks asset bundle definition for ds_reporting_dbricks.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: ds_reporting_dbricks
  uuid: 7f3ad231-8126-4fec-86a6-a5e147536996

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-4546775043599430.10.azuredatabricks.net
    variables:
      catalog: dev_datascience
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://adb-4546775043599430.10.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/dwhitney@rccl.com to make sure we only have a single copy.
      root_path: /Workspace/Users/dwhitney@rccl.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev_datascience
      schema: prod
    permissions:
      - user_name: dwhitney@rccl.com
        level: CAN_MANAGE
